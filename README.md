# mcp-core
A focused project for building and testing core MCP servers and clients from scratch. Implements key primitives — tools, resources, and prompts — across multiple domain-specific servers, paired with lightweight clients that drive real workflows. Covers stdio and HTTP/SSE transports, protocol conformance, and multi-server composition patterns.
